# Generated Binaries

This directory contains target-specific build outputs created by `pnpm build:native-launcher`.
The files are generated automatically for development and release builds and must not be committed.
Bundled engine binaries remain in `src-tauri/binaries` because they are produced by the separate Aria2 Next project.
